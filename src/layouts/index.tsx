import { FC, ReactElement } from "react";

const HomePageLayouts: FC = (): ReactElement => {
  return (
    <section className="flex items-center justify-center w-auto h-auto"></section>
  );
};

export default HomePageLayouts;
